import { Meta, Title, Subtitle, Heading, Source } from "@storybook/blocks";
import { useState } from "react";

<Meta title="Getting Started" />

<Title>Getting Started</Title>

<Subtitle>Learn how to install and use Pine UI in your project</Subtitle>

---

<Heading>Installation</Heading>

Install Pine UI using your preferred package manager:

<Source
	code={`npm install pine-ui-kit
# or
pnpm add pine-ui-kit
# or
yarn add pine-ui-kit`}
	language="bash"
/>

---

<Heading>Basic Usage</Heading>

### Import Components and Styles

<Source
	code={`import { ThemeProvider, Button, Badge, Switch, TextField } from "pine-ui-kit";
import "pine-ui-kit/style.css";`}
	language="tsx"
/>

### Use Components with ThemeProvider

<Source
  code={`function App() {
  return (
    <ThemeProvider theme="basic" defaultMode="light">
      <Button intent="primary" size="medium">
        Primary Button
      </Button>

      <Badge variant="solid" intent="success">
        New Feature
      </Badge>

      <Switch label="Enable notifications" />

      <TextField
        label="Email"
        placeholder="Enter your email"
        helperText="We'll never share your email"
      />
    </ThemeProvider>
  );
}`}
language="tsx"
/>

---

<Heading>Theme System</Heading>

Pine UI provides **three distinct design themes** that transform your entire UI with a single prop change. Each theme has its own unique visual language, supporting both light and dark modes.

### 🎯 Basic Theme

A clean, modern design system perfect for professional applications. Features smooth animations, subtle shadows, and a refined aesthetic.

<Source
	code={`import { ThemeProvider } from "pine-ui-kit";

function App() {
  return (
    <ThemeProvider theme="basic" defaultMode="light">
      <YourApp />
    </ThemeProvider>
  );
}`}
	language="tsx"
/>

### 🎮 Game Theme

Pixel-art inspired design for gamified experiences. Features retro aesthetics, bold borders, and playful interactions that bring a nostalgic gaming feel to your interface.

<Source
	code={`import { ThemeProvider } from "pine-ui-kit";

function App() {
  return (
    <ThemeProvider theme="game" defaultMode="light">
      <YourApp />
    </ThemeProvider>
  );
}`}
	language="tsx"
/>

### 🖍️ Crayon Theme

Soft, warm, and friendly design with hand-drawn aesthetics. Features rounded corners, gentle shadows, and a cozy feel perfect for creative or educational applications.

<Source
	code={`import { ThemeProvider } from "pine-ui-kit";

function App() {
  return (
    <ThemeProvider theme="crayon" defaultMode="light">
      <YourApp />
    </ThemeProvider>
  );
}`}
	language="tsx"
/>

### Theme Provider Props

- **theme** (optional): `"basic" | "game" | "crayon"` - The theme to apply (default: `"basic"`)
- **defaultTheme** (optional): `"basic" | "game" | "crayon"` - Initial theme for uncontrolled mode
- **onThemeChange** (optional): `(theme: ThemeName) => void` - Callback when theme changes
- **defaultMode** (optional): `"light" | "dark"` - Initial mode for uncontrolled mode
- **mode** (optional): `"light" | "dark"` - Controlled mode value
- **onModeChange** (optional): `(mode: ThemeMode) => void` - Callback when mode changes
- **syncWithSystem** (optional): `boolean` - Sync with system theme (default: `true`)
- **primaryColor** (optional): `string` - Custom primary color (hex format)

---

<Heading>Light & Dark Mode</Heading>

All three themes support both light and dark modes.

### Default Mode

<Source
  code={`import { ThemeProvider } from "pine-ui-kit";

function App() {
  return (
    <ThemeProvider theme="basic" defaultMode="light">
      <YourApp />
    </ThemeProvider>
  );
}`}
language="tsx" />

### Automatic System Theme Detection

You can enable automatic system theme detection:

<Source
	code={`<ThemeProvider syncWithSystem={true}>
  <YourApp />
</ThemeProvider>`}
	language="tsx"
/>

### Controlled Theme and Mode

For complete control over both theme and mode:

<Source
  code={`import { useState } from "react";
import { ThemeProvider } from "pine-ui-kit";

function App() {
  const [mode, setMode] = useState<"light" | "dark">("light");
  const [theme, setTheme] = useState<"basic" | "game" | "crayon">("basic");

  return (
    <ThemeProvider 
      theme={theme} 
      onThemeChange={setTheme}
      mode={mode} 
      onModeChange={setMode}
    >
      <div>
        <button onClick={() => setMode(mode === "light" ? "dark" : "light")}>
          Toggle Light/Dark
        </button>
        <button onClick={() => setTheme("basic")}>Basic Theme</button>
        <button onClick={() => setTheme("game")}>Game Theme</button>
        <button onClick={() => setTheme("crayon")}>Crayon Theme</button>
      </div>
      <YourApp />
    </ThemeProvider>
  );
}`}
language="tsx" />

### Custom Primary Color

You can customize the primary color across all themes:

<Source
  code={`<ThemeProvider theme="basic" primaryColor="#FF6B6B">
  <YourApp />
</ThemeProvider>`}
  language="tsx"
/>

---

<Heading>Design Tokens</Heading>

### Intent (Color Theme)

All components support these color intents:

- `primary` - Primary actions
- `secondary` - Secondary actions
- `success` - Success, completion
- `warning` - Warning, caution
- `danger` - Danger, deletion
- `neutral` - Neutral information

<Source
	code={`<Button intent="primary">Primary</Button>
<Button intent="success">Success</Button>
<Button intent="danger">Delete</Button>`}
	language="tsx"
/>

### Size

Most components support these sizes:

- `small` - Small
- `medium` - Medium (default)
- `large` - Large
- `xlarge` - Extra large

<Source
	code={`<Button size="small">Small</Button>
<Button size="medium">Medium</Button>
<Button size="large">Large</Button>`}
	language="tsx"
/>

### Variants

Components support different visual variants:

**Button variants:**

- `solid` - Filled button (default)
- `outline` - Outlined button
- `ghost` - Transparent button
- `weak` - Subtle button

**Badge variants:**

- `solid` - Filled badge (default)
- `outline` - Outlined badge
- `subtle` - Subtle badge
- `weak` - Weak badge

<Source
	code={`<Button variant="solid">Solid</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>`}
	language="tsx"
/>

---

<Heading>TypeScript</Heading>

Pine UI is written in TypeScript and provides full type support:

<Source
  code={`import type { ButtonProps } from "pine-ui-kit";
import { Button } from "pine-ui-kit";

const MyButton = (props: ButtonProps) => {
  return <Button {...props} />;
};
`}
language="tsx"
/>

---

<Heading>Examples</Heading>

### Login Form

<Source
  code={`import { useState } from "react";
import { Button, TextField, Checkbox } from "pine-ui-kit";

function LoginForm() {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [remember, setRemember] = useState(false);
  
  return (
    <form>
      <TextField
        label="Email"
        type="email"
        placeholder="your@email.com"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        fullWidth
      />

      <TextField
        type="password"
        label="Password"
        placeholder="password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        fullWidth
      />

      <Checkbox
        label="Remember me"
        checked={remember}
        onChange={(e) => setRemember(e.target.checked)}
      />

      <Button type="submit" intent="primary" fullWidth>
        Sign In
      </Button>
    </form>
  );
}`}
language="tsx"
/>

### Notification Badge

<Source
  code={`import { Badge } from "pine-ui-kit";
import { useState } from "react";

function NotificationIcon() {
  const count = 5;

  return (
    <div style={{ position: "relative", display: "inline-block" }}>
      <span style={{ fontSize: "1.5rem" }}>🔔</span>
      {count > 0 && (
        <Badge
          size="small"
          intent="danger"
          style={{
            position: "absolute",
            top: -5,
            right: -10,
          }}
        >
          {count}
        </Badge>
      )}
    </div>
  );
}`} language="tsx" />

### Form with Validation

<Source
  code={`import { TextField, Button } from "pine-ui-kit";

function ContactForm() {
  return (
    <form>
      <TextField
        label="Email"
        type="email"
        placeholder="your@email.com"
        helperText="We'll never share your email"
        status="default"
        fullWidth
      />

      <TextField
        label="Email (Error State)"
        type="email"
        placeholder="invalid-email"
        helperText="Please enter a valid email address"
        status="error"
        fullWidth
      />

      <TextField
        label="Email (Success State)"
        type="email"
        value="valid@email.com"
        helperText="Email is valid"
        status="success"
        fullWidth
      />

      <Button type="submit" intent="primary">
        Submit
      </Button>
    </form>
  );
}`}
language="tsx"
/>

---

<Heading>Styling</Heading>

### Custom Class Names

All components support the `className` prop:

<Source
	code={`<Button className="my-custom-class">
  Custom Styled Button
</Button>`}
	language="tsx"
/>

### Inline Styles

You can also use inline styles:

<Source
	code={`<Badge style={{ marginLeft: "8px" }}>
  Custom Badge
</Badge>`}
	language="tsx"
/>

---

<Heading>Layout</Heading>

Components support full-width layouts:

<Source
	code={`<div
  style={{
    display: "flex",
    flexDirection: "column",
    gap: "1rem",
  }}
>
  <Button fullWidth>Full Width Button</Button>
  <TextField fullWidth placeholder="Full width input" />
</div>`}
	language="tsx"
/>

---

<Heading>Troubleshooting</Heading>

### Next.js Integration

<Source
  code={`// app/layout.tsx
import { ThemeProvider } from "pine-ui-kit";
import "pine-ui-kit/style.css";

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <ThemeProvider theme="basic" syncWithSystem={true}>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}`}
  language="tsx"
/>

### Vite Integration

<Source
  code={`// main.tsx
import { ThemeProvider } from "pine-ui-kit";
import "pine-ui-kit/style.css";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <ThemeProvider theme="basic" defaultMode="light">
    <App />
  </ThemeProvider>
);`}
language="tsx"
/>

---

<Heading>Next Steps</Heading>

- [Components](/story/components-badge--default) - Explore all components
- [Introduction](/story/introduction--docs) - Learn about Pine UI
- [Changelog](/story/changelog--page) - View version history

---

Have questions? Check out our [GitHub repository](https://github.com/3o14/pine-ui-kit) or open an issue!
